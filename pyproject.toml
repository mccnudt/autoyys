[project]
name = "onmyoji-autobot"
version = "1.2.0"
description = "阴阳师通用战斗模板挂机（窗口识别 + FSM + 前台/后台输入）"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py39"
